-- -------------------------------------------------------------
-- Supabase Schema for Acquire OPD Submissions
-- Run this in your Supabase SQL Editor (https://database.new)
-- -------------------------------------------------------------

-- =============================================================
-- 1. CREATE TABLES IF THEY DO NOT EXIST
-- =============================================================

CREATE TABLE IF NOT EXISTS public.assessment_submissions (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at timestamp with time zone DEFAULT now() NOT NULL,
    name text NOT NULL,
    hospital text NOT NULL,
    mobile text NOT NULL,
    email text NOT NULL,
    city text NOT NULL,
    message text,
    total_score integer DEFAULT 0,
    score integer DEFAULT 0,
    answers jsonb DEFAULT '{}'::jsonb,
    knockouts jsonb DEFAULT '{}'::jsonb,
    status text DEFAULT 'Pending Review',
    review_notes text,
    user_ip text,
    user_agent text
);

CREATE TABLE IF NOT EXISTS public.submissions (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at timestamp with time zone DEFAULT now() NOT NULL,
    name text NOT NULL,
    hospital text NOT NULL,
    mobile text NOT NULL,
    email text NOT NULL,
    city text NOT NULL,
    message text,
    total_score integer DEFAULT 0,
    score integer DEFAULT 0,
    answers jsonb DEFAULT '{}'::jsonb,
    knockouts jsonb DEFAULT '{}'::jsonb,
    status text DEFAULT 'Pending Review',
    review_notes text,
    user_ip text,
    user_agent text
);

-- =============================================================
-- 2. ENSURE COLUMNS EXIST (IN CASE TABLES ALREADY EXISTED)
-- =============================================================

ALTER TABLE public.assessment_submissions ADD COLUMN IF NOT EXISTS status text DEFAULT 'Pending Review';
ALTER TABLE public.assessment_submissions ADD COLUMN IF NOT EXISTS review_notes text;

ALTER TABLE public.submissions ADD COLUMN IF NOT EXISTS status text DEFAULT 'Pending Review';
ALTER TABLE public.submissions ADD COLUMN IF NOT EXISTS review_notes text;

-- =============================================================
-- 3. GRANT PERMISSIONS TO ANON & AUTHENTICATED & SERVICE_ROLE
-- =============================================================

GRANT ALL ON TABLE public.assessment_submissions TO anon;
GRANT ALL ON TABLE public.assessment_submissions TO authenticated;
GRANT ALL ON TABLE public.assessment_submissions TO service_role;

GRANT ALL ON TABLE public.submissions TO anon;
GRANT ALL ON TABLE public.submissions TO authenticated;
GRANT ALL ON TABLE public.submissions TO service_role;

-- =============================================================
-- 4. ENABLE ROW LEVEL SECURITY (RLS)
-- =============================================================

ALTER TABLE public.assessment_submissions ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.submissions ENABLE ROW LEVEL SECURITY;

-- =============================================================
-- 5. APPLY FOOLPROOF PUBLIC INSERT & SELECT POLICIES
-- =============================================================

-- For assessment_submissions table
DROP POLICY IF EXISTS "Allow anonymous insertions" ON public.assessment_submissions;
DROP POLICY IF EXISTS "Allow public insertions" ON public.assessment_submissions;
CREATE POLICY "Allow public insertions" ON public.assessment_submissions
    FOR INSERT
    WITH CHECK (true);

DROP POLICY IF EXISTS "Allow public select" ON public.assessment_submissions;
DROP POLICY IF EXISTS "Allow authenticated select" ON public.assessment_submissions;
CREATE POLICY "Allow public select" ON public.assessment_submissions
    FOR SELECT
    USING (true);

DROP POLICY IF EXISTS "Allow public update" ON public.assessment_submissions;
CREATE POLICY "Allow public update" ON public.assessment_submissions
    FOR UPDATE
    USING (true);

DROP POLICY IF EXISTS "Allow public delete" ON public.assessment_submissions;
CREATE POLICY "Allow public delete" ON public.assessment_submissions
    FOR DELETE
    USING (true);

-- For submissions table (fallback)
DROP POLICY IF EXISTS "Allow anonymous insertions" ON public.submissions;
DROP POLICY IF EXISTS "Allow public insertions" ON public.submissions;
CREATE POLICY "Allow public insertions" ON public.submissions
    FOR INSERT
    WITH CHECK (true);

DROP POLICY IF EXISTS "Allow public select" ON public.submissions;
DROP POLICY IF EXISTS "Allow authenticated select" ON public.submissions;
CREATE POLICY "Allow public select" ON public.submissions
    FOR SELECT
    USING (true);

DROP POLICY IF EXISTS "Allow public update" ON public.submissions;
CREATE POLICY "Allow public update" ON public.submissions
    FOR UPDATE
    USING (true);

DROP POLICY IF EXISTS "Allow public delete" ON public.submissions;
CREATE POLICY "Allow public delete" ON public.submissions
    FOR DELETE
    USING (true);
